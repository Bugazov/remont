import React from 'react';
import { Link } from 'react-router-dom';

export const MainPage = () => {
    return (
        <div>
            <button><Link to = "/login">Выйти</Link></button>
            
        </div>
    );
};

